<td class="sf col-3"><%= f.input :source, collection: Skill::SOURCES, label: false %></td>
<td class="sf col-5"><%= f.input :name, collection: Skill::SAMPLE, label: false %></td>
<td class="sf col-2"><%= f.input :cost, label: false %></td>
<% if f.object.new_record? or current_user.admin? %>
  <td class="sf col-2"><%= link_to_remove_association "Remove Skill", f, class: "btn btn-danger"%></td>
<% end %>
