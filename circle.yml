machine:
  timezone:
    America/New_York

  ruby:
    version:
      ruby-2.3.1

  environment:
    DOCKER_COMPOSE_VERSION: 1.9.0
    COMMIT: ${TRAVIS_COMMIT::8}
    IMAGE: metricmike/simrep
    IMAGE_VERSION: $IMAGE:$COMMIT

  services:
    - docker

dependencies:
  pre:
    - curl -sSL https://s3.amazonaws.com/circle-downloads/install-circleci-docker.sh | bash -s -- 1.10.0
    - sudo pip install docker-compose

  override:
    - echo "Docker's got this."

database:
  override:
    - echo "Docker's got this."

compile:
  override:
    - docker-compose --verbose -f docker-compose.ci.yml up
    - docker-compose -f docker-compose.ci.yml run --rm web rake db:reset

test:
  override:
    - docker-compose -f docker-compose.ci.yml run --rm web rspec spec/
    # - docker login -e="$DOCKER_EMAIL" -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD" registry.heroku.com
    # - docker inspect $IMAGE

# deployment:
#   staging:
#     branch: master
#     heroku:
#       appname: simrep